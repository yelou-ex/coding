# 默认实现一览（哪个文件夹、哪条路线）

> 目的：**钉死当前默认使用的 RLWE / RGSW / LWE 分别在哪个文件夹**，避免混用。
> 判定依据是"代码实际 import 什么 + 运行脚本实际加载什么"，不是文件名。
> 最后核对：2026-09-19。路线定义见 `coding/RLWE路线审计.md`。

---

## 一、默认（路线 B：MPC4J 的 SEAL **Java 移植版**，已打补丁）

| 层 | 默认实现所在 | 具体文件 | 说明 |
|---|---|---|---|
| **RLWE** | 库在 `coding/lib/`<br>调用代码在 `coding/rgsw-lab/` | 库：`coding/lib/mpc4j-crypto-fhe-seal.jar`（+ `coding/lib/deps/` 11 个 jar）<br>调用：`rgsw-lab/src/main/java/com/fusepir/rgsw/Mpc4jRgsw.java` | jar 由 `mpc4j/mpc4j-crypto-fhe/mpc4j-crypto-fhe-seal/` 编译而来，**已打 Galois 惰性分配补丁**（`coding/patches/mpc4j-galois-lazy-permutation-tables.patch`），因此能跑到论文参数 |
| **RGSW** | `coding/rgsw-lab/` | `Mpc4jRgsw.java`（`encryptRgswConstant` / `externalProduct` / `cmux`）<br>`BlindRotateOps.java`（盲旋转 + LWE 索引加密） | 建立在上面那个 RLWE 之上；N=16384 下 5/5 全过 |
| **LWE** | `coding/lwe-java/` | 包 `cape.he`：`LWE`、`LWECiphertext`、`LWEParams`、`LWESecretKey`、`ModMath` | **唯一的通用 LWE 原语实现**。参数取自 Pirouette Table 4：d=512、σ²=3.192、q=2N、二进制密钥 ✓<br>⚠️ **修正**：原先写"MPC4J 全仓库没有 LWE"**不准确**——MPC4J 的 `cppir` 家族（ChalametPIR / SimplePIR / FrodoPIR / Piano / Plinko）本身就是 LWE/矩阵型 PIR，另有 `GaussianLweParam` 参数枚举；但它们**没有可复用的 `LWE.Enc/Dec` 原语** |

**默认怎么跑**：

```powershell
cd coding\rgsw-lab
.\run-mpc4j.ps1                                                  # RGSW/CMUX 自检
.\run-mpc4j.ps1 -Class com.fusepir.rgsw.Mpc4jCapability 16384    # 论文规模四项能力
.\run-mpc4j.ps1 -Class com.fusepir.rgsw.BlindRotateOps           # 盲旋转
cd ..\lwe-java; .\run.ps1                                        # LWE 自检（纯 JDK，无依赖）
```

---

## 二、已弃用（路线 C：自研 `com.fusepir.rlwe`）——**不是默认，别用**

| 位置 | 内容 |
|---|---|
| `coding/rlwe-java/` | 自研 RLWE 本体：`RingParams`、`RingOps`、`NttContext`、`RlweOps`、`RlweCiphertext`、`RlweKey`、`Gadget`、`RelinKeys`、`CiphertextOps`、`Serialization`（核心类带 `@deprecated`） |
| `coding/rgsw-lab/` 的 8 个文件 | `RgswOps`、`RgswCiphertext`、`MonomialOps`、`BootstrapKey`、`RgswLabMain`、`MonomialKeyTest`、`LabConfig`、`examples/RlweDemo`（**已全部加弃用横幅**） |
| `coding/rgsw-lab/run.ps1` | 驱动路线 C 的脚本（**顶部已加 `!! LEGACY - ROUTE C ONLY !!` 警示**），仅作交叉校验 |

弃用原因：缺"缩放回落"，密文×密文做不出可用结果；缺 BatchEncoder 与 Galois 密钥/密钥切换。

---

## 三、对照路线（路线 A：**真 SEAL 4.0.0 的 C++ 本体**，非默认）

| 位置 | 内容 |
|---|---|
| `coding/native-jni/` | JNI 封装 + 真 SEAL：`lib/mpc4j-native-fhe.dll`（4.43 MB，静态链入 SEAL）、Java 绑定 `SealStdIdxPirNativeUtils` / `Lpzl24BatchPirNativeUtils`、验证程序 `SealPirNativeTest` / `NativeApiProbe`、C++ 参数探针 `src/seal_params_probe.cpp` |

用途：**性能对照**与**交叉校验**（同参数同输入下两边结果应当一致）。已在 N=16384 跑通 SealPIR 全流程。

---

## 四、最容易混淆的六处

1. **两个都叫 `lib` 的目录，性质完全不同**
   - `coding/lib/` = **Java jar**（MPC4J 的 SEAL **Java 移植版**）→ 默认
   - `coding/native-jni/lib/` = **native DLL**（**真 SEAL C++** 的 JNI 封装）→ 对照
2. **`coding/rlwe-java/` 不是默认的 RLWE**。默认的 RLWE 调用代码在 **`coding/rgsw-lab/`**（`Mpc4jRgsw.java`），库在 `coding/lib/`。`rlwe-java/` 是自研弃用版。
3. **有两套 RGSW**：`Mpc4jRgsw.java`（默认）与 `RgswOps.java`（弃用）。
4. **`rgsw-lab/` 里有两个 run 脚本**：`run-mpc4j.ps1`（默认）与 `run.ps1`（路线 C，已加警示）。
5. **LWE 只有一份**：`coding/lwe-java/`。不要去找 MPC4J 的 LWE——它没有。
6. **"MPC4J 的 SEAL"有两种形态**：`coding/lib/mpc4j-crypto-fhe-seal.jar` 是**逐类翻译的 Java 重写**；`coding/native-jni/lib/mpc4j-native-fhe.dll` 是**真 SEAL 的 JNI 封装**。两者语义一致，但性能与内存上限不同。

---

## 五、测量/辅助工具（不是实现）

| 位置 | 用途 |
|---|---|
| `coding/param-probe/` | 走路线 B 的参数位宽探测；**已被 `native-jni/src/seal_params_probe.cpp` 取代**（后者直接问 SEAL） |
| `coding/rlwe-bench/` | 路线 C vs 路线 B 的性能对照基准 |
| `coding/pdf-extract/` | 三份 PDF 的按栏切分提取文本 |
| `coding/patches/` | 对第三方库的补丁（目前只有 Galois 惰性分配那一个） |
